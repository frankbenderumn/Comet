#ifndef COMET_COMMAND_PURGE_H_ 
#define COMET_COMMAND_PURGE_H_ 

#include "comet/command/command.h"

void purge(const char* package_name) {
    // if (command(pm, "purge") < 0) {
    //     return;
    // }
}

#endif