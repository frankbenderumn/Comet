#include "comet/command/install.h"

int main(int argc, char* argv[]) {

    Package* packages = 0;

    // if (install(packages, (char*)"prizm", "local-test/.internal/index", 126 + 12 + 1, "local-test") < 0) {
    //     BRED("FAILED TO INSTALL\n");
    //     exit(1);
    // }

    return 0;
}