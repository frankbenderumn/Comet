#include <iostream>

// #include "pugixml/pugixml.hpp"

int main(int argc, char* argv[]) {
    // Load the XML file
    // pugi::xml_document doc;
    // pugi::xml_parse_result result = doc.load_file("my_file.xml");

    // // Check for errors
    // if (!result) {
    //     std::cerr << "Error loading file: " << result.description() << std::endl;
    //     return 1;
    // }

    // // Traverse the XML tree
    // pugi::xml_node root = doc.child("root");
    // for (pugi::xml_node node = root.child("item"); node; node = node.next_sibling("item")) {
    //     std::string name = node.attribute("name").value();
    //     int value = node.attribute("value").as_int();
    //     std::cout << "Item: " << name << ", Value: " << value << std::endl;
    // }

    return 0;
}